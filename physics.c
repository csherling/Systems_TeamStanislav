#define PLAYER_HEIGHT 5
//struct arrow has initial velocity and angle, and current coordinates
typedef struct arrow{float velocity; float theta; float x; float y;}arrow;
typedef struct player{char health; float xcor;}player;

char shoot(/*arrow *a,*/ player *p, float v, float theta){
  float arrow_x=p->xcor;
  float arrow_y = 0;
  char unpassed = 1;//Checks if arrow passed the player
  while(a->y>=0){
    if((v>0)&&(arrow_x>p->xcor)&&(unpassed))
  }
}
int main(){
  return 0;
}
